# Testing the editor against the real games

What to try at the computer, in the order that proves the most with the least effort.
Each case says what to do in the UI, what the game should show, and how to get back to
vanilla. Automated suites cover the file formats (`Tools/test_*.py`); these cases cover
the one thing a test cannot - the shipped game reading what the editor wrote.

Before any of it: `crystal.exe` with no arguments finds the Steam installs, opens
the browser, and offers the projects menu. Nothing here needs a command line.

## FF4 3D (Steam)

Create a project first: **File ▸ New project**, target **FF4 on Steam**.

| # | Case | Steps | Expect | Undo |
| --- | --- | --- | --- | --- |
| F4-1 | Loose file: menu text | Text ▸ `files/babil_menu.msd` ▸ id **50002** "Inventory" ▸ rename ▸ Save ▸ Project ▸ Install | Main menu shows the new word | Project ▸ Remove; vanilla word back |
| F4-2 | Mass-file entry: dialogue | Text ▸ `files/t01_00.msd` ▸ id **581000** "So, you're Cecil." ▸ edit ▸ Save ▸ Install ▸ New Game | The Baron opening says the new line | Remove; check `CAST_EVENT_MSD.dat` size unchanged from original |
| F4-3 | Changes panel revert | With F4-1 and F4-2 both installed: File ▸ Changes… ▸ revert only the dialogue | Menu still modded, dialogue vanilla, in the game | Remove |
| F4-4 | Script edit | Scripts ▸ `files/d01_00.script` (Baron castle) ▸ in `func_676074621` change the first `startMessage` id to another id from the same msd ▸ Save ▸ Install | The NPC says the other line | Remove |
| F4-5 | Table edit | Tables ▸ `files/item_parameter.pak.lz` ▸ weapons ▸ row 1 (id 6001, aggressivity 10) ▸ set aggressivity to 99 ▸ Save ▸ Install | Equip that weapon; Attack on the status screen reflects it | Remove |
| F4-6 | Menu layout | Menus ▸ `files/MenuLayout_Root.xbn` ▸ move a frame by 8px ▸ Save ▸ Install | The main menu's element has moved | Remove |
| F4-7 | Exit | Maps ▸ `d01_00` ▸ select a door ▸ change its destination to `d01_02` ▸ Save ▸ Install | Walking through lands on the other map | Remove |
| F4-8 | Add a character | Maps ▸ `d01_00` ▸ Add character ▸ pick a model, position, and a line ▸ Save ▸ Install | The new NPC stands there and talks | Remove |
| F4-9 | Uninstall integrity | After any of the above, Remove, then Steam ▸ Properties ▸ Verify integrity | Steam finds 0 files to reacquire | - |

## FF3 (Steam)

Target **FF3 on Steam**. The chest test (Ultima Weapon in the first chest) already
passed; these are the rest.

| # | Case | Steps | Expect | Undo |
| --- | --- | --- | --- | --- |
| F3-1 | Text | Text ▸ `files/menu.msd` ▸ any visible menu word ▸ edit ▸ Save ▸ Install | Menu shows it | Remove |
| F3-2 | Table | Tables ▸ `files/item_parameter.pak` ▸ weapons ▸ Attack of the starting knife ▸ Save ▸ Install | Status screen shows the new Attack | Remove |
| F3-3 | Exit | Maps ▸ `d01_01` ▸ a door ▸ change destination ▸ Save ▸ Install | Door leads elsewhere | Remove |
| F3-4 | Delete a character | Maps ▸ `d01_01` ▸ select an NPC ▸ Delete ▸ Save ▸ Install | NPC gone, nothing else broken (talk to the others) | Remove |
| F3-5 | Sound | Audio ▸ any BGM ▸ play in the browser | Plays, loop point shown | - |
| F3-6 | Integrity | Remove everything, Verify integrity | 0 files | - |

## Both games, editor behaviour

| # | Case | Expect |
| --- | --- | --- |
| E-1 | File ▸ Open project switching FF3 ↔ FF4 | Script editor completion changes to the other game's instruction set without a reload (FF4 has `setInsideMapJump`, FF3 does not) |
| E-2 | Project with an FF4 target opened while FF3 install missing (rename the folder briefly) | A clear message naming the missing install, not a crash |
| E-3 | Install while the game is running | A clear refusal or a note that the game must be restarted; no half-written container |
| E-4 | Revert a file the game has since updated (touch the container) | Skipped with a reason; the file is left alone |
| E-5 | Two projects for the same game, install A then B | B's files replace A's; Remove leaves vanilla, not A |
| E-6 | Project targeting both games (Settings ▸ tick both) | Two tabs above the libraries; FF3 and FF4 badges on document tabs; `#/ff4steam/...` in the address bar |
| E-7 | Open a map from each game, drag one tab beside the other | Both render, each with its own textures (no 404s in the console) |
| E-8 | Edit a text line in FF4, then File ▸ Changes… | The FF4 tab in the dialog lists it; the FF3 tab is empty; Project ▸ Install — FF4 on Steam installs only that |
| E-9 | File ▸ Export as .zip… | A zip beside the projects folder with project.json, the per-target files, and a README; Explorer opens on it |
| E-10 | Models ▸ `n441` (FF3) - the motion bar picks `★ n441` and plays `n441_101_01` | The figure animates smoothly for 90 frames and loops; scrubbing the timeline and the arrow keys step frames; switching to a `b_f…` pack of another skeleton distorts it (expected - different bones) |
| E-11 | Models ▸ a monster, e.g. `b_m005`-family model with `b_f005.ncap` | The battle idle plays; compare against the game's battle screen for direction and speed |
| E-12 | Models ▸ `n441` with `n441_101_01` playing ▸ Export .glb ▸ open in Blender (File ▸ Import ▸ glTF 2.0) | The chocobo appears textured with an armature of 31 bones and an action of 90 frames that plays the same walk; UVs and vertex colours intact |
| E-13 | Maps ▸ any map (FF3 `d03_02`, FF4 `d01_03`) after a change to the model viewer's shader | Terrain, characters, the gizmo and the exit boxes all draw, not just the exit tags. The scene view shares the viewer's vertex shader, which now skins through `palette[mindex]`; the scene pins `mindex` to 0 and writes its matrix to `palette[0]`. A uniform that no longer exists fails silently in WebGL (no console error), so an empty view with tags is this regression |
| E-14 | Start `crystal` with no project, both games installed | Two tabs above the libraries, FF3 and FF4, both live and each once (the OpenFF and Steam targets of a game read the same install, so only one session per install opens): the editor behaves as a project targeting every game. Open an FF3-only project (`Text`): the FF4 tab stays but greyed, its tooltip saying the project does not target it; clicking it opens Project settings; tick FF4 there and it comes alive |
| E-15 | Maps ▸ FF4 `f00` (and FF3 `f01_44`, any `fNN_XY`) | The field draws as its grid of chips (16×16 for FF4's f00, textured from the field's sheet), with the objects and exits over it; an FF3 chip map frames its own chip inside the whole world. A **mirror z** box appears in the toolbar: on for FF4 (its layout), off for FF3. On f00 with mirror z on, Baron castle's object (o200) stands on the castle plain and the 33 exits sit on coasts, towns and cave mouths; with it off they fall on sea and mountains - the measurement behind the client's FieldMirror. The ground must read as smooth grass with soft coast and desert edges: the coast and second-grass tiles are flagged flip (mirrored repeat) with coordinates past the texture edge, and plain repeat turns the whole world into a hard-edged patchwork |
| E-17 | Client: `OpenFF.exe --content="<FF4>" --map=f00 --pos=-596,0,-150` (Baron town's world exit, as FF4's script writes it) and `--pos=-280,0,-60` | Cecil on the plain before the cave mouth with the mountain range standing behind it; at the second spot the town object with its pond and the surrounding mountains. The camera looks from the far side (`--fieldmirror=off` shows FF3's placement instead: Cecil inside a coastal range). Flattened relief with snow-coloured slivers in the grass means the camera is on FF3's side again |
| E-16 | Client: `OpenFF.exe --content="<FF4>" --map=f00 --pos=-596,0,-150` and `--content="<FF3>" --map=f01 --pos=0,0,1` | FF4's lowlands blend smoothly (the flip wrap, `BindTextureWrap`); FF3's grass and water look as they always did - FF3's materials leave every wrap bit clear and expect repeat, so clamp is never chosen. `--texwrap=off` restores the old always-repeat for comparison |
| E-18 | Crystal ▸ Export to OpenFF | A project targeting our build (Project settings) with an edited file; Project menu ▸ Export to OpenFF… (or the button beside Export as .zip) | The mod appears as `mods/<name>/` beside the OpenFF client (`OpenFF.exe` records where it is in `%LocalAppData%\OpenFF\launch.json`; with no record, the OpenFF build beside the repository is used), with `mod.json`, `ff3/files/` (and `ff4/files/` when FF4 is ticked under OpenFF too; since 2026-09-07 - an older export's `files/` is cleared) and a README; the status line names the folder and the file count and Explorer opens on it. Exporting again replaces the files; a folder of that name without a `mod.json` is refused rather than overwritten. A project not targeting our build is refused with a pointer to Project settings. Start the client: the edit is live |
| E-19 | Crystal ▸ C# code for a mod | A project targeting our build; Project menu ▸ Add C# code… (or the button beside Export to OpenFF); then Build C# code; then Open C# code in editor; then Export to OpenFF | Add writes `code/<Name>.csproj` (referencing the client's `OpenFF.Engine.dll`, never copying it), `code/Mod.cs` (a GameService that logs map entries, with a spawn-a-villager example in comments) and a `.gitignore`; the status line names the csproj. Build runs the .NET SDK and reports `built <Name>.dll` - or the first error line, file and position, with the rest in the browser console. Open launches whatever the machine opens .csproj files with (Visual Studio, Rider, VS Code). Export copies the built assembly and its .pdb to the mod's root and lists it under `assemblies` in `mod.json`; the client's log then has `engine: mod <id> ...: 1 service(s)` and `<Name>: started`. Rebuild while the client runs: it hot-reloads. Add a second time is refused with the csproj's path |
| E-20 | The sample mod, installed | Double-click `Samples\HelloMod\install.cmd` (or run it) | It builds the sample into the client's mods folder beside the repository and says so; the title's MODS list shows "Hello mod" on the next start. Rebuild with it while the client runs and the client hot-reloads. It stops with a message when the client has not been built |

## The client

| # | Case | Steps | Expect |
| --- | --- | --- | --- |
| C-0 | Start with nothing | `OpenFF.exe` (no arguments), then `OpenFF.exe --game=ff4`, then `OpenFF.exe` again | The first boots FF3 from Steam and writes `%LocalAppData%\OpenFF\launch.json` with both install paths; the second boots FF4 (Baron town); the third boots FF4 again because the choice is remembered. `--source=content` boots our extracted Content; `--content=<dir>` ignores the file. The log's `launch:` line names game, source and path |
| C-1 | Boot from Steam alone | `OpenFF.exe --content="C:\Program Files (x86)\Steam\steamapps\common\Final Fantasy III"` (nothing else; the log's `content:` line must name only the install) | Logos, then the title with the logo at the phone's size, the three menu words and the hand cursor; `steam cells: phone placement applied to title_gousei_new.NCER` in the log |
| C-2 | Play from Steam alone | New game from C-1: naming, the opening, the first battle, walk out of Ur, talk to an NPC, open the menu | Everything the archive build does, with no exception in the log; windows, HP bars, icons and the cursor sized as on the phone build; the about screen (title ▸ about) is blank rather than a crash |
| C-2b | Steam-only really is Steam-only | Rename `Project\Content` briefly, run C-1 and C-2 | Identical; nothing in the log mentions `Content\` |
| C-2c | Steam's own layouts | Add `--steam-cells-off` to C-1 | The title logo draws about a third too large and the copyright strip moves - the reason the table exists |
| C-3 | A project as a mod | `OpenFF.exe --project=<your project>` after the chest test | The Ultima Weapon chest edit is live without installing anything; the log lists the override in use |
| C-4 | Mod folder | Copy one edited file into a folder mirroring `files/...`, `OpenFF.exe --mod=<folder>` | The edit is live |
| C-5 | TrueType text | `OpenFF.exe --content="<Steam FF3>" --size=1600x960`, New Game, read the opening dialogue; then the same with `--text=atlas` | Sharp Arial at window resolution vs the blurry 16px atlas; the text sits in the same place in both; menus line up |
| C-6 | A face of your own | `--font=C:\Windows\Fonts\georgia.ttf` | Dialogue in Georgia; widths still consistent (right-aligned numbers in menus stay aligned) |
| C-7 | Steam's sound | Boot from Steam with `--log=file`; listen through the title and the opening | Music and effects play; the log lists `sound: sound/BGM00_1.ogg -> 43.4s` and friends, no `Content.Load` of a sound XNB; the intro (`_0`) hands over to the loop (`_1`) without a gap |
| C-8 | Regenerating the layout table | `python Tools/gen_steam_cells.py <extracted files/> "<Steam FF3>"` | `29 cell banks`; only `mastercard.NCER` skipped; the json under `OpenFF/Data` is unchanged (git shows no diff) |
| D-1 | FF4 boots into Baron | `OpenFF.exe --content="C:\Program Files (x86)\Steam\steamapps\common\Final Fantasy IV" --size=1600x960` | Within ~10 s: Baron castle's grounds (stone platform, moat, bridge) with Cecil in dark-knight armour and his shadow, at the world-map arrival; the castle theme plays; the log says `content: loose files + mass files: 11361 files ...; then synthesised movement tables (2 files)` and `script: FF4 table built - 200 of 500 commands` |
| D-2 | Any map, any game | `--map=d01_01` on FF4 (castle interior), `--map=t01_01` on FF3 (Ur), `--map=d01_01 --pos=0,0,10 --rot=90` | The named map with the party at the position; for FF3 the game plays normally from there (NPCs talk, exits work); for FF4 walking works, NPCs stand where the .hich puts them |
| D-3 | The porting list | Boot FF4 with `--log=general,file --log-file=ff4.log`, walk around, read the log | `missing:` lines name FF3 files FF4 has no equivalent for yet (2D banks, `WorldDefine.xbn`, `eureka_*.msd`); `script: FF4 command N ... not implemented` lines name the commands the map used; no `Exception` lines |
| D-4 | Probe | Add `--probe` | Every 3 s a `probe:` line with vertices > 0, `cameraMtx` not all zero, `bounds` a few hundred units wide; `--noscript` keeps the map, `--noanim` keeps it too |
| D-6b | Exits follow the script | Boot FF4 with `--log=general`; watch for `exits: d01_00 declares j001 -> f00 (1 now)` lines as the map's script runs, not one summary at load; then edit a map script in Crystal so a `setInsideMapJump` sits behind a flag test and install it | The exit appears in the log only when the flag branch runs; the door does nothing until then |
| D-6 | Walking between FF4 maps | Boot FF4 (D-1), walk north along the path to the castle gate (touch-drag or WASD), through the arch | The screen fades and Cecil arrives inside Baron castle (`d01_01`) at the door; the log says `exits: j002 -> d01_01 at (216,24,-213) facing 180` then `exits: d01_01 has 4 scripted exit(s)`; walking back out returns to the grounds; `--map=d01_01` alone starts at the same door |
| D-7 | Tapping an NPC | Boot FF4 with `--probe`, walk up to an NPC in the open and tap them | The log shows `touch: (x,y) vs <model> at (...) r=8 HIT`; when the leader is touching them a talk starts (FF3 mechanics: message text through TrueType, no window art yet for FF4) |
| D-8 | An event map | `--map=e01_00` on FF4 with `--log=general` | The Red Wings deck renders and stays up; the log lists the `ce_*` cutscene commands as `not implemented - skipped` and no `Exception` |
| D-9 | FF4 dialogue | `--map=t01_00 --pos=0,0,72 --say=581000,100500` on FF4 | After ~4 s a navy window with a white frame at the bottom reads "So, you're Cecil." in TrueType, with "Cecil" as the speaker's name; the same window and text appear when a townsperson is touched and tapped |
| D-10 | FF4 motions | Boot FF4, stand, then walk (drag or WASD) | Standing plays the idle (hand on hip), walking plays the walk cycle; no T-pose. FF4 numbers its motions 1000/1001/1002 where FF3 asks 1001/1004/1005; `GameProfile.FieldMotionId` renumbers them as a pack registers |
| D-11 | FF4 overworld | `--map=f00` on FF4; wait ~10 s; also `--map=f00 --pos=-280,0,-40` | Mountains, grass and sea with Cecil on the ground; by the castle position a settlement is in view; walking streams new chips in; the log's `jump:` line names a chip (`f00_67`), never bare `f00` |
| D-5 | FF3 unchanged | C-1, C-2 and the archive boot | Identical to before (the FF4 work is behind `GameProfile`, which reads the content's shape) |
| C-9 | World map and menus from Steam alone | From C-2 reach the world map; open the menu, status, items, config | The ship and map markers (`w_map_*`, `map_marker_*`), volume slider (`m008_volume`) and menu icons (`icon_8dot`, `icon_16dot`, `m015_bar`) are placed and sized as on the phone build |
| C-10 | Steam's menu layout: the hand and the labels | Boot from Steam, continue a game, open the menu; then Quicksave | The hand stands wholly left of the command panel, level with the word it points at (Item, Magic...), never over the word or straddling the panel's edge; the labels sit in the middle of their rows, not against the top. In the quicksave dialog the hand is left of Yes/No, level with the word. With `--debug=boxes` the yellow cross (where the code puts the hand) is left of the focused frame and the hand's green sprite box ends before the frame begins. Steam's MenuDefine uses alignment 6 for these lists, which the phone code did not know (`SteamLayout`, `MBText.bmGetCursorX`, `Medget.cursorY`) |
| C-11 | Steam's title | Boot from Steam and wait for the title | "Press ANY BUTTON to START" is centred on the screen (the log's `title:` line says which columns of its 300 px strip paint and where it was placed; the words are what is centred, not the strip). The title menu has three entries - NEW GAME, CONTINUE, LOAD GAME - and no blank fourth line the hand can land on (Steam's bank keeps cells 4 and 5 empty; `SteamCells.CellHasPicture`) |
| C-12 | Quicksave | Menu ▸ Quicksave ▸ Yes; again with No | The first tap on Yes moves the hand to it, the second confirms: the screen fades to black, comes back on the "saved" message, a tap returns to the title, and `save.bin` under `%LocalAppData%\IsolatedStorage` has a new modified time. No closes the dialog back to the menu. Steam's layout names the answers `YES`/`NO` where the code listens for `yes`/`no`; frame ids now compare without regard to case (`Medget.IdEquals`), so a dialog that stays open after either answer is this regression |
| C-13 | Debug overlay | Any boot; press F1. Or `OpenFF.exe --debug=all --screenshot-every=3 --screenshot-dir=<dir>` | F1 shows a layer over the frame: in the corner the running part, stage and its type, whether the field is mirrored, the hero's position (and spot on a chip field), the focused frame and where its cursor goes, fps and frame time, draw calls and vertices, memory, viewport. F2 outlines every menu frame (yellow = focused, with a cross at the cursor position), F3 writes their ids and sizes, F4 outlines every 2D sprite as drawn (green sub plane, magenta 3D plane), F5/F6 toggle the world and stats text; the last line always lists the keys with each part's state (`F2 boxes [on]`...). The screenshots of a `--screenshot-every` run include the layer, so a headless run can be read from its pictures. Nothing in the game changes while it is up; F1 again hides it |
| C-14 | The mods folder | Make `mods/` beside `OpenFF.exe` with two mods, each a folder with `files/` mirroring the game's names (one file both carry, e.g. a text `.msd`); start the client | The log's `mods:` line lists both, in alphabetical order, with their file counts, and a second line names the shared file and which mod wins (the first). `mods/loadorder.json` is written with both entries enabled. Swap their order in the file, restart: the other wins. Set `"enabled": false` on one: it is skipped (the File channel says why) |
| C-15 | Mods by target | Give one mod a `mod.json` with `"target": "steam"`, leave another with no `mod.json` at all | The Steam-targeted one is skipped and the File channel says `targets steam` (a Steam mod is a file replacement Crystal installs; the client does not load it). The manifest-less folder loads, named after its folder. An OpenFF mod is never tied to one game: it applies whether FF3 or FF4 was booted, since under OpenFF the booted game is an asset source |
| C-16 | The exe on its own | Copy the client build (`dotnet build -o <folder>`) to a folder with no Content directory anywhere above it and run it with no arguments | It boots FF3 from Steam: the game install is the working directory, the log has the `launch:` line and no "could not locate the Content directory" message. Before this, only a build inside the repository, where Content is a few folders up, could start without `--content` |
| C-17 | A mod with code | `Samples\HelloMod\install.cmd` (or `dotnet build Samples/HelloMod -o "<client>/mods/hello"`), start the client, F1 | The log has `engine: mod hello 1.0: 1 service(s), 1 behaviour type(s)`, `hello v1: OnGameStart`, then `part ... -> TITLE` and so on as the game moves; on entering a map `entered d01_05 (DUNGEON, ff3)`, `OnSceneLoaded`, `Spinner.Start on hello-marker #1`; every 600 frames a heartbeat. The overlay's world layer shows `engine 0.1.0.0 mods 1 services 1 objects 1 ...`, the mod, the service's line and the spinner's yaw. `--nomods` loads no code (`engine: --nomods`) while the mod's files still apply |
| C-18 | Hot reload | With C-17 running on a map, change `Greeting` in HelloService.cs to `hello v2` and rebuild into the same folder | Within a second: `mod hello: reloading`, `Spinner.OnDestroy`, `hello v2: OnGameStart (frames so far N)` with N the old instance's count (the public fields were handed over), the overlay says `reloaded x1`. The file was never locked: the build's copy succeeds while the game runs. A build that does not load leaves the mod out with a message, not a crash |
| C-19 | A mod's save chunk | C-17, Continue, then Menu ▸ Quicksave ▸ Yes; restart and Continue again | On Yes: `engine: save: slot 41568 (13846 bytes) - 1 chunk(s) to %LocalAppData%\OpenFF\saves\mods.json`, and that file holds the slot with the mod set (`hello 1.0`) and `hello/HelloService` v1 with Frames, MapsEntered, LastMap. On the next Continue: `engine: load: slot 41568 - 1 chunk(s)` and `hello v1: loaded chunk v1 - frames N` - the counts come back with the game. Remove the mod and Continue: the chunk is reported as kept, not deleted |
| C-20 | Mod dependencies | Give a second mod a `mod.json` with `"dependencies": [ { "id": "hello", "minVersion": "2.0" } ]` | It is skipped and the File channel says `needs hello 2.0 or later, has 1.0`; with `"minVersion": "1.0"` it loads after hello; place it before hello in `loadorder.json` and it is skipped with `must come before it in the load order` |
| C-21 | The in-game mod list | Title screen: the fourth entry reads MODS (drawn by the client; Steam has no picture for it). Tap it, or move the hand to it and press Enter | A panel lists every mod in the mods folder in load order with [x]/[ ], version, what it brings (files, code) and why one is skipped (a failed dependency, a Steam target). Up/Down or W/S select, Space/Enter toggle, Shift+Up/Down (or PageUp/PageDown) move, Esc or the Back button closes. The mouse does the same: a row toggles, its ^ and v move it, the wheel scrolls. Closing after a change writes `mods/loadorder.json` and the log says so with "applies at the next start"; closing without a change logs "nothing changed". While the list is open the title does not react to keys or taps |
| C-22 | The engine API: a mod acts on the game | Build `Samples/HelloMod` into the mods folder (C-17), Continue into a map, press A (Z) beside the villager | On entering the map the log has `engine api: spawned n011 as character N` and the mod's `villager spawned ... next to the hero`; a villager stands two units from the hero, facing them, and does not shove the hero (spawned characters are non-solid unless `Npc.Solid` is set). Walk away and the villager walks over (`villager walking over`), with the walk motion, and turns to the hero when near. Press A within three units: the sound plays, the message window shows the mod's text with the tap-to-continue mark, gil rises by 10 (`talked to the villager (1), gil now ...`); tap to dismiss and talk again. The overlay's world layer shows the villager's position and `(talking)` while the window is up. `Game.Hero.Position` and `Game.Field.Map` read what the overlay shows |
| C-23 | The engine API, second slice: a scene | The sample mod (C-17), talk to the villager | The hero freezes and turns to the villager; "Hello from a mod..." and a tap; then "Would you like 10 gil?" with the game's own Yes/No box (up/down or a tap picks, A or a tap answers, B is no); Yes shakes the camera and pays, No puts a "!" over the villager's head; the villager then walks two legs around the hero, facing where it walks, turns back to the hero, and control returns. The log shows the whole scene ran as one coroutine ("talk N done") and, as you play on, the game's own events: `flag G/I set`, `a battle starts - party Luneth L1 ...`, `cutscene started/ended` around scripted scenes, `the game showed message N`, `item X xN` from chests |
| C-24 | Camera and effects from a mod | In a mod: `Game.Camera.MoveTo`, `LookAt`, `Follow`, `Zoom`, `Reset`; `Game.Effects.Spawn(category, member, position)` | MoveTo and LookAt free the camera and place it; Follow returns it to the hero; Zoom changes the field zoom as the scripts' SetCamera_ZoomDegree does; Reset restores the map's camera. Spawn returns the effect id and the effect plays at the point; Remove ends it |
| C-25 | A mod draws and reads input | The sample mod on a map; press H | Top right, a black bar with yellow text - gil, talks, party size - and under it a bar that is green while the villager is within talking reach and red when not, redrawn every frame (`Game.Draw.Rect/Text`, `Game.Input.Pressed(Pad.L)`). H hides and shows it (`Game.Input.KeyPressed("H")`; the pad's buttons are the game's: X and a shoulder button open the menu, Select toggles encounters, so mods take keyboard letters). In a mod, `Game.Input.Capture = true` takes every key and tap away from the game: the hero does not walk, the menu does not open, while the mod reads `Game.Input` (pad held/pressed/released, pointer in 800x480 screen units, keys by name); `Capture = false` gives them back |
| C-26 | Models, encounters, battles from a mod | In a mod: `Game.Npcs.SpawnModel("b_m005", pos)`, `Game.Field.Encounters = false`, `Game.Battle.Start(monsterParty)` | A monster model stands where placed and can be moved, turned, scaled, hidden and removed like a spawned character (no AI, no talk of its own); with Encounters off no random battle starts while walking (on again restores them); Battle.Start goes into the game's battle with that monster party and, on return, BattleEnded reports Won, Lost or Escaped (also after ordinary encounters) |
| C-27 | World to screen and ground height | The sample mod on a map with the HUD on (H) | "Villager" stands over the villager's head and stays there as the camera and the villager move (`Game.Camera.WorldToScreen`); the HUD's "ground" figure equals the hero's y (within a unit) on flat ground and differs on stairs while the hero is mid-step; `Game.Field.GroundHeight` is null off the map's collision (over a pit, outside the walls) |
| C-28 | Spells and monsters as data, the formulas | The sample mod on a map; press J | The game's Fire effect and sound play on the villager, a battle-style damage number pops over it, the screen flashes orange. The log has "cast Fire (power N, effect c/m -> id ...) at the villager as Goblin (L.., .. hp): N damage" with the hero's intellect and job skill, then a line listing the first spells with their levels, the first monsters with their HP, and the hero's charges per level and equipped spell ids; "encounter group 1 = party 1: ..." names the monsters of that encounter. Without a Fire in the tables (never on a map) the log says so instead of crashing |
| C-29 | Changing the party from a mod | In a mod: `Game.Party.Hurt(0, 5)`, `Heal`, `SetHp`, `SetCharges(0, 1, 3)`, `GiveExperience`, `SetJob(0, Job.BlackMage)`, `SetStat`, `LearnSpell(0, fire.Id)`, `Inflict(0, Condition.Poison)` / `Cure` | The main menu shows the change at once (HP, job with its bonuses, the spell in the level-1 slots, the poison mark); Hurt stops at 1 unless canKill; Heal does not raise the dead unless revive; a job change keeps the game's own bookkeeping (abilities, penalty time) |
| C-30 | Behaviours from a scene file | The sample mod installed (its scenes/d01_05.json attaches Welcome to the map); enter the Altar Cave | Two seconds after the map appears a message window says "The Altar Cave. The hello mod put this line here from a scene file."; the log has "mod hello: 1 object(s) from scenes/d01_05.json" and "Welcome on d01_05/map said ...". F1 lists d01_05/map with its Welcome line. Rebuild the mod while on the map: the objects are remade (the line is said again if EveryTime) |
| E-21 | Crystal ▸ Behaviours on a map object | A project targeting our build, with C# code built (a Behaviour class with public fields in it); open a map, select a character, an exit, or Terrain | The inspector ends with "Behaviours (OpenFF)" and an **Add Behaviour** button; it drops a list with a search box (Unity's Add Component): a row per Behaviour class with an icon and its name, the summary as tooltip; typing filters, arrows and Enter pick, Escape closes. Picking one adds a card - icon, name, an open-the-source button, × - with its public fields as inputs (numbers, text, checkboxes, enum lists, x/y/z for vectors, a list of strings one per line; `[Header]` groups, `[Tooltip]` on hover, `[Range]` a slider, `[ItemField]` the item list) filled with the code's defaults; a change writes scenes/<map>.json a moment later by itself (the status line says "saved"; there is no Save button; the Project tree's change count moves). Without code the section is an "Add C# code" button; a class written but not built is listed "not built", attaches, and its card says so until Build. The list heads with the engine's own under *Built in* - Chest, Talk, Trigger - and the mod's under its name |
| E-22 | Crystal ▸ Run in OpenFF | A project targeting our build; File > Run in OpenFF (or the button in Project settings) | The project is exported into the client's mods folder (files, code, scenes) and OpenFF.exe starts if it is not running; if it is running the status line says so and the client hot-reloads the code. With no client found the item is disabled and its note says why |
| E-23 | Crystal ▸ OpenFF API reference | File > OpenFF API reference… | A dialog lists the engine's types (Game first, then IHero, IDialogue, IMagic..., then Behaviour, GameObject...) with every member's signature and summary from OpenFF.Engine.xml; typing in the search box narrows to matching types and members |
| C-31 | Battle motions on the field, items, shops | In a mod: `Game.Hero.BindBattleMotions(); Game.Hero.PlayMotion(HeroMotion.MagicShot)`; on a monster `npc.BindMotions(monster.MotionSet); npc.PlayMotion(MonsterMotion.Attack)`; `Game.Items.Find("Bronze Sword")`, `Game.Party.Items`, `Game.Party.Equip(0, id)`; `Game.Shops.Open(0, "t01")` | The hero plays the battle's casting motion on the field (arms out; the field's own wait motion does not cut it short) and MotionDone turns true when it ends; the goblin swings its battle attack; the item comes back with its price, slot, attack and jobs, the bag lists what the party carries with counts, Equip puts the item in the hero's hand (the menu shows it) and false when the job cannot wear it; the game's shop screen opens with the first town's weapons, buying and selling work, IsOpen is true until it closes - and it is a map trip: the field leaves for the shop interior (MapLeaving fires, spawned characters go) and returns to the map's shop-exit spot |
| C-32 | Goblin Survivors | Install `Samples\Survivors\install.cmd`; on a map press T | Goblins come in waves and swing when they reach the hero (red flash, a number over the hero); the hero turns to the nearest and throws Fire on their own (a number over the goblin, its health bar shrinks); kills log "+N exp, +N gil"; a run level darkens the screen with three cards - Left/Right + A or 1/2/3 picks one and the HUD's stats line changes (power %, every Nf, spell); some goblins leave a chest that gives equipment when walked into ("Found ... - equipped"); after a wave a trader stands in front of the hero: talk, Yes opens the mod's own shop list (the first town's wares at the game's prices; Up/Down, A buys and equips when better, B leaves), No starts the next wave; at 1 HP the run ends with a message and T starts another |
| E-24 | Crystal ▸ the mod's own objects in the 3D view | An OpenFF project; open a map in 3D; select Terrain, press "Add an OpenFF object where the camera looks" (or "+ add an object" under Objects (OpenFF) in the hierarchy, or Add a game object ▸ *OpenFF object*) | A small blue box with a facing sliver appears at the view's centre and is selected; the inspector is laid out as Unity's: the name in a box at the top, then a Transform card (Position X Y Z, Rotation Y, Scale), a Model card (the picker; "None - a spot with logic on it"), Tags, Parent, Behaviours, Children, Delete, and the map's own facts folded under "About d01_05" at the bottom; dragging the gizmo arrows moves it (the fields follow), the turn ring sets the yaw; clicking it in the view selects it; the hierarchy lists it under Objects (OpenFF) and marks anything that carries behaviours with a ◆. Pick a model (o001): the box becomes the chest, drawn in the view, washed blue while selected, picked by clicking on it; the × beside the model takes it away again. Rename it: the attachments on it follow. Every change writes scenes/<map>.json by itself a moment later (`objects: [...]`, yaw/scale/model/tags/children; the status line says "saved") - no Save button anywhere in the panel; Delete removes it, its children and their behaviours |
| E-35 | Crystal ▸ a chest with no code | On an object with model o001: Add Behaviour ▸ Chest (under *Built in*); pick Potion under Item, Count 2, Gil 100; Export to OpenFF; boot the map and talk to the chest (A) | The card shows Contents (Item as the game's item list, Count as a slider 1-99, Gil) and Opening (Once, Message, Empty Message) under headings, with tooltips; the file has `"behaviour": "Chest", "fields": { "Item": 5001, "Count": 2, "Gil": 100 }`. In the game the chest says "Found Potion x2 and 100 gil!" (the log: "chest d01_05/Chest: Potion x2 and 100 gil"), the bag has two more Potions and the party 100 more gil; talking again says "The chest is empty."; a save and load keeps it open (SceneMemory, chunk openff/scene). An object without a model and a Chest on it opens when the hero walks within Radius. Talk on an object says its Lines one window at a time with the Speaker's name |
| E-34 | Crystal ▸ children, the hierarchy's menu and drag, and an older scene file | On the object from E-24: right-click it in the hierarchy ▸ New child object; set the child's z to 6; drag the parent in the view and turn it; drag the child's row onto the group header; drag it back onto the parent's row; right-click ▸ Duplicate, ▸ Rename, ▸ Delete; open a project whose scene file has `points` | The menu offers New child object, Duplicate, Rename, Focus in view, Move to top level (greyed at the top level), Delete (with children). The child appears under the parent in the hierarchy, indented, and in the 3D view 6 units ahead of it; its Transform card says "relative to <parent>" and keeps x 0 z 6 while the parent moves and turns - the child follows in the world (yaw 90 puts it 6 along +x). Dropping the row on the group puts it at the top level where it stands (its numbers become world ones); dropping it on a row makes it that row's child, again in place; the target lights up while the drag is over it. Duplicate makes "<name> 2" beside it with the subtree and the behaviours copied; Rename puts the caret in the name box; the right-click selects the row first. An older file's points list as objects without a model; a save writes them as `objects` and the file's `points` go |
| C-33 | The mod's objects in the engine | A mod with scenes/<map>.json holding objects (from E-24 via Export or Run in OpenFF), one with model o001 and a child carrying the engine's Trigger; Goblin Survivors on a map with objects tagged "spawn" | Each object is a GameObject named `<map>/<path>` (`d01_05/TreasureChest`, `d01_05/TreasureChest/Trigger`) tagged "scene" and its own tags at its world position, with a MapObject (Kind "scene", Name, Path, Model); the child's Parent is the parent, the parent's Children hold it. The one with a model stands there as a plain character: "engine api: placed model o001 as character N at x, y, z", MapObject.Npc set; the log has "mod <id>: N object(s) from scenes/<map>.json" counting the whole tree. Walking into the trigger's Radius logs "trigger <map>/<path>: hero entered" and publishes Events.TriggerEntered; leaving, "left". Leaving the map destroys them all (the spawned model with them) before the next map's file applies. A file with `points` and `point:<name>` targets still reads. Survivors spawns its goblins at the objects tagged "spawn" (the log says "from N spawn point(s)") |
| C-34 | FF4: the opening scene's characters, and Baron town's script | `OpenFF.exe --game=ff4 --map=e01_00`; then `--game=ff4` (Baron town) | On the Red Wings deck Cecil and the crew appear and play their scene motions (the scene's characters go through the character manager); the log has "FF4 cutscene starts on e01_00" and, some twenty seconds later, "FF4 cutscene ends", with no "outside the table" line between them - only camera motions, effects and the scene battle are logged as skipped. In Baron town the log has no "not implemented" line at all: door swings, footstep dust and BGM ducking are skipped quietly, confirm boxes draw the engine's Yes/No, and the name window shows speakers' names |
| C-35 | FF4: the opening scene as staged | `OpenFF.exe --game=ff4 --map=e01_00 --screenshot-dir=<dir> --screenshot-every=2` for a minute | The camera plays the scene's own motions (the log: "FF4 camera set e01_00 in slot 0: 19 motion(s)", then one "camera motion N from e01_00, F frames" per shot, each lasting its F frames at 30/s): the deck from astern with the sails, Cecil's close-up while the crew speak, the crew from above, then the sepia Mysidia flashback (the hall, the soldiers, the throne stairs - the yellow is the script's display mask) and back to the deck. The stage animates with it: "map motion pack e01_00 (23940 bytes)", the deck props and clouds move and hide per shot (visibility animations); Cecil's eyes and mouth change with the lines (face pack p00_001 from FACE.dat; no "did not load"). The scene's music plays (BGM02 in slot 3: "sound: files/SOUND/BGM/BGM02.akb"), its sound effects fire, and every line is voiced ("FF4 voice Ev01_000a (0.6s)" per line from SOUND/VOICE/en_*.akb, Japanese where no English file exists; `--novoice` mutes them) with ce_EndVoice holding the script until the line ends. Every ce_ command in the scene runs or is quiet; only `setEffect_Scale`, the battle BGM and the scene's battle stay logged as skipped. `--ff4cam=off` leaves the field camera alone (the shots collapse, the map still animates). Since the second look (2026-09-05, night): the sky dome stands behind the deck in every shot (the scene clip is 2..4096), the soldiers stay in the wide shots (`ce_SetEnbleViewClip`), no disc lies on the deck's centre or the hall's floor with nobody over it (the casts' shadows follow their `kosi` joint; the field hero is hidden), the "Cecil / Lord Captain / Baron Red Wings" plate fades in, holds solid and fades out without pulsing (sprite alpha 0..31), and the shots keep the script's FOV - 30 degrees unless `eventCameraSetFovyMove` says otherwise - rather than each motion's own (the File log has no "asks a N-frame slide" line: no shipped shot blends). Use `--nomods` for clean pictures: the sample mod's HUD draws over the scene. With A tapped through the fight and on into e01_01 (the story drive does it): "e01_00 was left unnoticed - its scene state released before e01_01 starts", then e01_01's slots 0..5 as characters 2..7 and "bind object p02_01b as character 8 at slot 4's L_wepon"; every "skipped N command(s)" line names only "(quiet)" entries - a name without "(quiet)" is a behaviour the scene lacks; no "quiet sets list" warning at the table build |
| C-36 | FF4: a new game plays the opening | `OpenFF.exe --game=ff4` (no --map) | The client boots into Baron castle's throne room (t00_00, the map FF4's NewGameInitPart names, at the origin); its script hands out the starting items (an FF3 handler fed FF4 ids used to crash: now "threw ... skipped from here on" once in the log) and calls the opening: "story scene e01_00, back to e01_01 at (5,0,-5)", the airship scene plays as in C-35, "scene battle 900 skipped ... on to e01_01", e01_01 swaps its stage to e01_18 ("ce_SetMap e01_18 replaces e01_01 within the scene") with Cecil and the crew still up, and its ce_setConteNextPart sends the chain back: "cutscene ends" then "entered t00_00". Tapping A through the castle: Baigan greets Cecil in a close-up, the throne room (t00_05) shows the king on his throne with the guards along the carpet and Cecil kneeling before him, the "Baron" plate fades in over the airship and the castle's over t00_06 - the map scripts' moveCamera/setCamera_*Gaze/setCameraOffset drive the field camera directly (Ff4EventCamera), where FF3's MODE_FREE left every shot at the party's feet. Left tapping A, the story runs on through the throne room (the king's questions, Kain's entrance) and hands control back in the castle hall with no crash, "outside the table" or loud skip in fifteen minutes; `addItem` with FF4 ids is the one handler still logged once. `--map=<any>` skips the flow and boots that map as before. The deck scenes speak over FF4's dark, translucent message bar across the bottom (text centred), which goes away between lines and at the scene's end; the castle talks keep the framed window with the name tag |
| E-25 | The unified tables (OpenFF.Data) read both games | `crystal tables "<FF4 install>" --items=30`, then the same with the FF3 install | "ff4 tables: 99 levels, 15 characters, 303 items, 123 spells, 252 monsters"; the experience curve starts 16, 47, 105, 204; Cecil (Dark Knight, marked * as a tentative name) is 40 hp at level 1 and about 220 at level 10 with str 13; items carry their names and captions from babil_item.msd ("Potion (Consumable, 30 gil) - Restores 100 HP.") and weapons their attack and hit ("Dark Sword ... atk 10 hit 85"); no "note:" lines. Monsters list with level, hp, experience and gil and their drops ("Goblin L10 (30 hp, 5 exp, 90 gil) drops Potion 819; Tent 492..."). The FF3 install answers "ff3 tables: 99 levels, 4 characters, 282 items, 127 spells, 255 monsters" ("Goblin L1 (7 hp, 1 exp, 10 gil)") with the same experience curve, Luneth..Ingus, "Potion (Consumable, 50 gil) - Restores 50 HP." from eureka_item.msd, and the one note that FF3 grows by job. The same classes (Shared/Data, namespace OpenFF.Data) compile into the client |
| E-26 | FF3's jobs and spells in the unified tables | `crystal tables "<FF3 Steam install>" --game=ff3` | "23 jobs", then one line per job: "job 17 Black Belt: curves 0,0,2,6,6,0; L1 str 5 ...; L30 str 30 agi 24 vit 30 int 15 spi 15 hp ~1094 charges 0/0/..." and "job 21 Sage: ... charges 15/13/11/9/8/6/4/2" - the growth curves, the level-30 attributes, the expected hit points and the magic charges per level from player.chaindata, named from eureka_menu.msd 50105+. The 127 spells carry level (Cure 1, Tornado 8), power, hit rate, use kind and element from the 52-byte magic records |
| E-27 | Crystal ▸ Game data | Project tree ▸ Game data ▸ double-click Jobs (FF3) or Characters, Spells, Monsters, Encounter groups, Shops, Efficacies (FF4) | A read-only grid of the unified tables with named columns: Jobs lists the 23 jobs with growth curves, level 1/30/99 attributes, expected hit points and charges; Spells the level, MP, power, hit rate, element and target bits; Monsters attack/defence/drops with item names; Encounter groups their members and placements; Shops the rows of babil_shop.bbd with wares and prices. The filter box narrows rows, a column header sorts (again to flip), the count reads "n of m"; the note above the grid names the file and offsets behind the page. On the Steam workspaces the names are the games' own; on our own build the character and job names carry a * (tentative) where its text files were not found |
| E-28 | Crystal ▸ the OpenFF mod folder | Open `SceneTest` (an OpenFF project with code and a scene); look at the bottom of the project tree; drag the bar between the tree and the file list | The tree ends with a pinned **OpenFF mod** folder holding **Code** and **Scenes** (with a count); it stays put when the tree scrolls. Code lists `Mod.cs`, the `.csproj` and the `.gitignore` - never `bin/` or `obj/`; double-click opens the file in a pane with line numbers and colouring, no *unsaved* mark until something is typed, Tab indents and Shift+Tab outdents the selection, Ctrl+S saves (the file keeps its CRLFs), the address bar reads `#/code/code/Mod.cs` and a reload comes back to it. **New file…** offers Behaviour, GameService and an empty class and refuses a name that exists; **Build** with a deliberate error lists it under the strip with file, line and message and a click lands on the line; fixed, "built SceneTest.dll". The tree's width follows the bar and is kept across reloads |
| E-29 | Crystal ▸ a scene is a map | `SceneTest` ▸ OpenFF mod ▸ Scenes ▸ `d01_05` | The row's inspector names the map, its behaviours and points, and offers **Open map** and **Open as JSON**. Double-click (or Open map) opens the *map editor* on d01_05 - the 3D view, hierarchy with Points (OpenFF), the Behaviours section in the inspector - on the project's OpenFF workspace; Open as JSON opens `scenes/d01_05.json` as text in the code pane. In the Maps library, d01_05 carries a small scene mark |
| E-30 | Crystal ▸ one mod, both games | File ▸ New project…; in the grid tick FF3 and FF4 under **OpenFF mod** (the default when both are installed), Create; edit a text line in each game; Scenes ▸ Export to OpenFF | The panel has two tabs, **FF3** and **FF4**, with no kind under the names (each game is open once); the libraries switch between the games' content. The File menu has Export to OpenFF and Run in OpenFF under an *OpenFF* heading and no Install / Remove, since nothing is installed anywhere; `/api/mod/status` says so. The export writes `mods/<name>/mod.json`, `ff3/files/` and `ff4/files/` (the status line counts both) and a README that says which folder applies to which game. Tick FF3 under Steam too: the FF3 tab grows an *OpenFF* / *Steam* subtitle and the two appear as separate tabs |
| E-32 | Crystal ▸ a new behaviour from the inspector, and the cog | `SceneTest` ▸ d01_05 ▸ select a character ▸ Add Behaviour ▸ type `Greeter` ▸ Enter; then the cog beside the project's name in the header | The list's last row reads `New Behaviour "Greeter"`; Enter writes `code/Greeter.cs` (a Behaviour with Start/Update from the starter), attaches it to the character (a card "Not built yet - Build the C# code and its fields appear here"), and opens the file in the code view; the tree's Code row is highlighted and lists it. Build, reselect the character: the card has the class's fields. The cog's tooltip names `code/Mod.cs`, the mod's GameService, and clicking it opens the file; on a project with code but no GameService class it writes `code/Mod.cs` (or `Service.cs`) from the service starter and opens it; with no code it adds the C# project. A Steam-only project has no cog. Double-clicking a scene with the inspector still fetching leaves no error in the console |
| E-33 | Crystal ▸ the session comes back | `SceneTest`: open a map, a code file and a script, focus the map, put the panel on Text; reload the page; then File ▸ Open project ▸ `Test`, and back to `SceneTest` | After the reload the three tabs are back with their contents, the map focused (its 3D view drawn), the panel on Text; `session.json` in the project folder lists them (`docs`, `active`, `browse`, `ws`). Opening `Test` closes them and shows its start page with **Open project** over `Test v1.0` and the other projects folded under *Switch to another project…* (Test itself not among them); `SceneTest`'s session.json is unchanged by the switch, and opening it again brings its three tabs back. A single click (preview) is not kept; a tab whose file has gone is skipped without an error. Export as .zip does not include session.json |
| E-31 | Crystal ▸ a Steam mod sees the OpenFF path | Open `Test` (FF3 on Steam only); click **OpenFF mod** at the bottom of the tree | The strip offers **Make it an OpenFF mod…**, which opens Project settings; the note says why (the client plays it, and it may carry code). No Code or Scenes rows until an OpenFF game is ticked |
| C-40 | Per-game files in a mod | In `mods/<m>/ff3/files/` a changed `menu.msd`, in `mods/<m>/ff4/files/` a changed `babil_menu.msd`, in `mods/<m>/files/` a third file; boot `--game=ff3`, then `--game=ff4` (Docs/Drives/ff3-boot.drive for the first) | FF3's log applies `ff3/files/` and `files/` and never the FF4 folder; FF4's applies `ff4/files/` and `files/`; each game's own text shows the change. `--project=<dir>` on a project targeting both OpenFF games reads the booted game's target folder the same way (`files/` for the first target in project.json, `targets/<target>/files` for the other). The ff3-boot drive ends with "drive: ff3 boot done" |
| C-38 | FF4's party on the unified layer | `OpenFF.exe --game=ff4` (a new game); in a mod, `Game.Party.Members`, `Game.Party.Items` | The log has "party: ff4 tables: 99 levels, 15 characters, 303 items, 123 spells", "party: new game - 1 in the party ... Cecil L10 220/220 hp 27/27 mp", then from the castle script "+1 Red Fang (now 1)" and "Cecil equips Dark Shield, Dark Sword, Dark Helm, Dark Armor, Dark Gloves"; when Kain joins, "party: Kain joins - 2 in the party" and later "leaves". Through the engine API the members carry FF4's numbers (level, hp, mp in Charges[0], stats with equipment bonuses) and the bag its items; SetJob does nothing (fixed classes). FF3 still answers from pl.PlayerParty |
| C-39 | The OpenFF menu on FF4 | `OpenFF.exe --game=ff4 --map=t01_00 --party=4:10`; press the menu button (C on the keyboard, or M) | FF4's own menu look (Steam's window art and glove): on the left five rows with Cecil and Rydia - portrait, name, Lv, HP x / y, MP x / y; on the right the commands in FF4's order - Inventory, Magic, Equipment, Abilities, Status, Party, Settings, Quicksave, Save, Load (six visible, a bar for the rest) - and below them the place ("Baron" once the map's plate has shown it, else t01_00) and "500Gil". Up/Down move the glove; A on Status sends the glove to the party rows, Up/Down pick, A opens Status: title bar, portrait, name, Lv, class (Dark Knight / Summoner), HP, MP, the five attributes and six combat values in the layout's rows, EXP and For next level, the five equipment slots; Right shows the next member; Z opens Abilities; X returns. X on the root closes, so does C or M. Settings and Quicksave say they come later |
| C-40 | The OpenFF battle on FF4 | `OpenFF.exe --game=ff4 --map=f00 --pos=-596,0,-150`; press K, then A (Z) through the fight | the party jumps to battle stage b01 (the Baron plain's; the log: "battle: to stage b01, back to f00 ..." then "stage: b01"); "Encounter!" - a Goblin and a Sword Rat (FF4's m000_00 and m001_00 models by family, idle motion) stand on the left, Cecil on the right, a side-view camera at the stage's end, panels show the foes (bottom left), Cecil with HP and an ATB gauge (bottom right) and, when the gauge fills, Attack / Magic / Items / Run in FF4's window layout (M runs); Attack then Left/Right + A picks a target: Cecil swings, a number pops over the monster, its health bar shrinks, "Cecil hits Goblin for 18." scrolls at the top; the monsters strike back ("Sword Rat hits Cecil for 9", red flash); when both fall FF4's result window at the top shows Gil Found / New Total and EXP (drops and level-ups in a second window) and the party (C-38/C-39) shows the gil, the Potion and the experience; the party jumps back to the field where it stood ("battle: back to f00") and control returns. The log has "battle: 2 foe(s): Goblin (30 hp), Sword Rat (31 hp) against Cecil L10" and "battle: won - ...". Damage is a placeholder formula (attack x2 - defence, +-10%) |
| C-49 | FF4 menu: equipment and item use | `OpenFF.exe --game=ff4 --map=t01_00 --party=4:10 --gil=5000`; O, buy a Thunder Rod and a Healing Staff, Leave; C (menu) | Equipment, then A on Rydia: her five slots (Right/Left/Head/Body/Arms) with what is worn; Up/Down pick a slot, A lists what in the bag fits it with Attack or Defense (a weapon's position bits 1 right hand / 2 left hand, armour's 4 head / 8 body / 16 arms, and the item's canEquip bit for her type - Cecil cannot take the rod), Remove first when something is worn; A equips, the old item returns to the bag, her Attack changes on the Status screen. Inventory: A on a Potion asks "Use on whom?" and heals the member picked ("Potion: Rydia +100 HP" in the notice and log); a Phoenix Down only takes a fallen member; a Tent "cannot be used here" until camping exists. Drive: `Docs/Drives/ff4-equip.drive` (log: "menu: Rydia equips Thunder Rod (right)") |
| C-48 | FF4 inns | `OpenFF.exe --game=ff4 --map=t01_30 --gil=500` (Baron's inn, 50 gil); talk to the keeper | After the keeper's line a Yes/No box "Stay (N gil)" / "No" (`bootInn`); Yes with enough gil takes the gil and the script goes on to `setRecovery2` (everyone's hit and magic points restored - log "setRecovery2 - n member(s) fully restored") and `setConditionRecovery`; No, or too little gil ("I'm afraid you're short on gil."), jumps to the keeper's other line (`selectEndWait`). Drive: `Docs/Drives/ff4-inn.drive` walks up from the door blind - if the keeper stands elsewhere, walk to them by hand |
| C-47 | FF4 shops on the unified layer | `OpenFF.exe --game=ff4 --map=t01_00 --gil=5000`; O (a test key: Baron's weaponsmith, row 1 of babil_shop.bbd) - or walk into Baron's weapon and armour shop (t01_40, rows 1 and 2) or item shop (t01_60, rows 3 and 40) and talk to the keeper (`bootShop`) | An engine-drawn shop: the keeper's title and line ("Weaponsmith", "How might I be of service?"), Buy / Sell / Leave. Buy lists Thunder Rod 700, Healing Staff 480, Flame Claws 350, Ice Claws 450, Lightning Claws 550 with the bag count; Left/Right set the count, A buys ("Thank you!"; "I'm afraid you're short on gil." when not); Sell lists the bag at the record's selling price (Thunder Rod 350); Leave or B closes and the keeper's script goes on. Drive: `Docs/Drives/ff4-shop.drive` (log: "shop: open row 1 'Baron Weapon' (Weaponsmith)", "shop: bought 2 x Thunder Rod for 1400", "shop: sold 1 x Thunder Rod for 350", "shop: closed with 3950 gil") |
| C-46 | FF3 still boots and plays (regression drive) | `OpenFF.exe --game=ff3 --map=d01_05 --drive=Docs/Drives/ff3-boot.drive --log=general,file` | Ur loads ("stage: d01_05 model..."), the drive walks, opens and closes the menu and quits; no "Exception:" line in the log. `Docs/Drives/ff4-story.drive` is the FF4 counterpart: three minutes of the opening with A tapped, through the scene battle (900) and on to e01_01/e01_18, no exceptions |
| C-45 | FF4 physical blows and hit rolls | `OpenFF.exe --game=ff4 --map=t01_00 --party=4:10`; K; Fight | "Cecil hits Goblin for 25..29" (his bare strength 13 x level 10 x attack 6 over the Goblin's defence 20 + level 10 + vitality 9, times 1.0..1.3, times 1.2 onto a monster); "Goblin hits Cecil for 21..27" with no armour on (attack 7 x level 10 x strength 9 over 0 + 10 + 11, times 0.7 onto a member); the odd "X misses Y" from the hit roll (weapon hit + agility - evade - agility + 20, out of 100). Equipment from the castle scenes lowers the Goblins' blows. Blizzard on a Goblin now lands 55..70 (its magic defence 5 is in the divisor) |
| C-44 | A scripted drive plays a test without focus | Lock the desktop or leave the window in the background, then `OpenFF.exe --game=ff4 --map=t01_00 --party=4:10 --drive=Docs/Drives/ff4-battle-magic.drive --log=general,file` | The game plays the file's steps by itself - "drive: 54 step(s)", "drive: press K", the fight's lines, "drive: fight over", "drive: quit" - and exits; the whole run takes about a minute. An `until` that never matches logs "drive: timed out" and the drive carries on. Steps: wait, press <key> [ms], until <regex> [s], say, quit |
| C-43 | FF4 magic in the OpenFF battle | `OpenFF.exe --game=ff4 --map=t01_00 --party=4:10` (Cecil with the child Rydia at level 10); K; when Rydia's gauge fills (her name starred) Down, A opens Magic | Her list reads Cure 3, Hold 5, Confuse 5, Esuna 15, Blizzard 5, Thunder 5... with the MP cost at the right (magic_parameter.bbd; her learn list from player.chaindata chain 21 at level 10). Cure asks "Cure on whom?" over the party panel and heals ("Rydia casts Cure: Rydia +9" in the log, +0 at full health); Blizzard asks for a Goblin and takes it down in one ("Rydia casts Blizzard: Goblin takes 82" - power 20 x level 10 x wisdom 6 over the Goblin's will, level and magic defence, times 1.0..1.3); her MP falls by the cost in the party panel. Cecil, a Dark Knight, gets "Cecil knows no magic." A Potion in the bag heals 100 and an Ether gives 50 MP (efficacy.beld), a Phoenix Down only takes a fallen member. Levels won teach spells ("Rydia learns Fira!" when her list says so). The songs do not appear for anyone but Edward (type 11) |
| C-42 | FF4 save and load on the unified layer | `OpenFF.exe --game=ff4 --map=t01_00` (Baron town; a dungeon may throw an encounter in the way); walk a little; open the menu (C or M); Right twice to the Save page; A on slot 1; close. Then start `OpenFF.exe --game=ff4 --load=1` | The Save page lists "Slot 1  Cecil L10 - t01_00, 0 gil  <date>" and "Saved to slot 1."; the log: "save: field - t01_00 at 5,0,-290 rot 32768, 9 flag(s)" then "save: FF4 slot 1 - ...". The second start logs "party: restored - 1 in the party ... Cecil L10 220/220 hp", "load: field - t01_00 at 5,0,-290", "jump: ff4 -> t01_00 at the saved spot" and "load: landed on t01_00 at 5,0,-290 (saved 5,0,-290)"; Cecil stands where he was saved with the party, bag, gil and script flags as they were. The menu's Load page (Right three times) does the same in play through a map jump. The store is `%LocalAppData%\OpenFF\saves\ff4.json` (FF3's chunks stay in mods.json); the legacy game's own write at map entry shows up there as slot 41568 and is harmless. Not saved yet: NPC state, the scene chain, `SetStat` overrides on a member (the growth table wins) |
| C-41 | FF4 encounters and encounter groups | `OpenFF.exe --game=ff4 --map=d01_00` (the Watery Pass); walk | Within some dozens of steps "Encounter!" with one of the map's set-0 groups (the log: "encounters: d01_00 rate 9 (by land form 9/9/...), set 0 groups 16,17,18,19 ..." then "battle: encounter group 16" - Sword Rat, Goblin, Sword Rat in the game's own placements; 17 and 18 are Tiny Mages, 19 three Fangshells). Towns have rate 0 and no fights; on the overworld the chip under the party decides ("encounters: f00_48 rate 1 ... set 0 groups 370,372,374" on the Baron plain: a Floating Eye, a Helldiver, three Goblins). The land form under the party (which set and rate apply) is not read yet - set 0 stands for all. K on any map fights group 1; a script's `bootEventBattle(group, ...)` and `Game.Battle.Start(group)` start the same fight; `Game.Monsters.Group(id)` lists a group's members |
| C-50 | FF4 battle stage round trip, camera and party | `OpenFF.exe --game=ff4 --map=t01_00 --party=4:10 --drive=Docs/Drives/ff4-battle-magic.drive --log=general,file --screenshot-dir=shots --screenshot-every=2` | The drive presses K on the Baron town square: the log has "battle: to stage b01, back to t01_00 at 5, 0, -250 after", "stage: b01", "battle: camera at 0, 45, 240" (FF4's own standing shot for camera type 0, after a short slide from (0, 32.7, 166)), the fight, "battle: won", "battle: back to t01_00", "stage: t01_00", "drive: back on the field", and no Exception. The screenshots during the fight show the b01 plain with its mountain backdrop across the top, the Goblins left, and BOTH Cecil and Rydia right as FF4's battle models (Cecil at the top slot, Rydia below - the front row of battle_parameter.chain's party root), the party rows and command window drawn from FF4's own window frames (the blue bevel of window_frame_00), its glove cursor at the picked row and its ATB gauge ("ff4 ui: no sheet ..." in the log would mean a fallback to drawn rectangles); Rydia's spell list is one wide three-column grid; after the fight the town again with Cecil where he stood. A map without a stage for its land form (a scene's fight, or a stage id over 30) fights in place with "battle: no stage bNN in the content" |
| C-51 | FF4 menu tour | `OpenFF.exe --game=ff4 --map=t01_00 --party=4:10 --gil=500 --drive=Docs/Drives/ff4-menu.drive --log=general,file --screenshot-dir=shots --screenshot-every=2` | The log has "menu: open", "menu: Status of Cecil", "menu: Abilities of Rydia" (Right switched member), "menu: Inventory", "menu: Equipment of Rydia", "menu: Party", "menu: close", "drive: menu tour done", no Exception; the screenshots show the root with the party rows and the command column, the Status screen with the attribute rows, the empty inventory ("Nothing in the bag."), the Equipment screen with "Nothing in the bag fits there." |
| C-37 | FF4: the follow camera on a town map | `OpenFF.exe --game=ff4 --map=t01_00` | Cecil stands at Baron town's gate with the camera behind and above him, the FF4 way (leader + (0, 90, 85), looking at leader + (0, 17, 5) - FF4's WorldCamera defaults; FF3's own offsets framed the gate's roof with Cecil out of view). Walking keeps him framed |

## What "works" looks like in the log

The console pane shows every install as `wrote <file>` or `rebuilt <container> (<n>
entries, one replaced)`. A backup of anything replaced sits in
`<override>.backup`, and `installed.json` beside it lists every file with hashes.
